import { Routes } from '@angular/router';
import { LinesListingComponent } from '../lines/lines-listing/lines-listing.component';

export const routes: Routes = [
  { path: '', component: LinesListingComponent }
];
